// filepath: c:\Users\infin\Documents\Flutter Projects\armor\android\app\build.gradle
plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin"
}

android {
    namespace = "com.example.armor"
    compileSdk = 36  // Updated from 34 to 36
    ndkVersion = flutter.ndkVersion

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_17  // Updated from 1_8
        targetCompatibility = JavaVersion.VERSION_17
    }

    kotlinOptions {
        jvmTarget = JavaVersion.VERSION_17  // Updated from 1_8
    }

    defaultConfig {
        applicationId = "com.example.armor"
        minSdkVersion flutter.minSdkVersion
        targetSdk = 36  // Updated from 34 to 36
        versionCode = flutter.versionCode
        versionName = flutter.versionName
    }

    buildTypes {
        release {
            signingConfig = signingConfigs.debug
        }
    }
}

flutter {
    source = "../.."
}
